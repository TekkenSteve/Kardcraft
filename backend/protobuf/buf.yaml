version: v1
deps:
  - buf.build/bufbuild/protovalidate
lint:
  use:
    - BASIC
  except:
    - DIRECTORY_SAME_PACKAGE
    - PACKAGE_DIRECTORY_MATCH
    - PACKAGE_SAME_DIRECTORY
breaking:
  use:
    - FILE
